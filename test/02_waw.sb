/*
    UF:
    inteiro: 3
    add: 3
    mul: 3
    INST:
    lw: 1
    mul: 10
    sub: 2
    div: 40
    add: 2
*/

.data
.text
    # Exemplo de uma lista de instrucoes que so tenha apenas a dependencia WAW
    # entre as instrucoes 
    
    mul r1, r8, r8
    sub r2, r4, r5 
    mul r3, r4, r3
    add r3, r4, r4

    exit
