/*
    UF:
    inteiro: 2
    add: 2
    mul: 1
    INST:
    lw: 1
    mul: 10
    sub: 2
    div: 40
    add: 2
*/

.data
.text
    # Exemplo de uma lista de instruções que só tem a dependência WAR
    sub r2, r6, r5 
    mul r1, r8, r8
    mul r3, r6, r4 # lê r4 (war)
    add r4, r6, r6 # escreve em r4...
    exit